/*! jQuery v3.6.0 | (c) 2021 jQuery Foundation, Inc. | jquery.org/license */
/*!
 * This is a placeholder file. In production, use:
 * <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
 * 
 * Or download actual jQuery from: https://jquery.com/download/
 */

// For local development without internet, this acts as a fallback
// Replace with actual jQuery library from https://code.jquery.com/jquery-3.6.0.min.js

console.warn('jQuery placeholder loaded. Replace with actual jQuery from https://code.jquery.com/');

// Basic jQuery compatibility shim (minimal - use real jQuery in production)
window.jQuery = window.$ = function(selector) {
    if (typeof selector === 'string') {
        return document.querySelectorAll(selector);
    }
    return selector;
};

// Add basic jQuery methods
if (typeof jQuery !== 'undefined') {
    jQuery.post = function(url, data, callback) {
        fetch(url, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(data)
        })
        .then(r => r.json())
        .then(callback)
        .catch(e => console.error(e));
    };
}

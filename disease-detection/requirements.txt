# Smart Chashi - AI Disease Detection
# Use build.sh on Render (installs CPU-only torch automatically)
# Local Windows: run.bat handles everything

# Web Framework
flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0

# Production WSGI server
gunicorn>=21.0.0

# Image Processing
pillow>=10.0.0
numpy>=1.24.0

# ML/AI (PyTorch — EfficientNet-B0 disease classifier)
# On Render: build.sh installs CPU-only version (~200MB vs 2GB CUDA)
torch>=2.0.0
torchvision>=0.15.0

# ML/AI (Transformers — ViT plant detection model)
transformers>=4.35.0

# Utilities
python-dotenv>=1.0.0
